npm start
ng serve -0

cmd +c to stop the server

@Component:
@NgModule: where the declarations are done

FIRST CREATE sub COMPONENT:

1. ADD THE TEMPLATE:
	 IN THE MAIN COMPONENT
2. ADD DECLARATIONS IN MODULE


Component
	Template:

		Inline template:
		LinkedTemplate:
			has own html and then use it int template:
	Class //typescript
	metadata

ngfor="let product of products"
	gett the values


Packge.json all the dependency
Anglura.json confi for testing bulding project

Index.html: final HTML which renders 

App.module.ts: new component and servives which create the component